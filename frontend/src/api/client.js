import axios from 'axios';\n\nconst API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000/api';\n\nconst api = axios.create({\n  baseURL: API_URL,\n});\n\n// Add token to requests\napi.interceptors.request.use((config) => {\n  const token = localStorage.getItem('token');\n  if (token) {\n    config.headers.Authorization = `Bearer ${token}`;\n  }\n  return config;\n});\n\n// Auth API\nexport const authAPI = {\n  register: (email, password, name) =>\n    api.post('/auth/register', { email, password, name }),\n  login: (email, password) =>\n    api.post('/auth/login', { email, password }),\n};\n\n// Customers API\nexport const customersAPI = {\n  getAll: (limit = 50, offset = 0, segment = null) =>\n    api.get('/customers', { params: { limit, offset, segment } }),\n  getById: (id) => api.get(`/customers/${id}`),\n  create: (data) => api.post('/customers', data),\n  update: (id, data) => api.put(`/customers/${id}`, data),\n  updatePreferences: (id, preferences) =>\n    api.put(`/customers/${id}/preferences`, preferences),\n  delete: (id) => api.delete(`/customers/${id}`),\n};\n\n// Campaigns API\nexport const campaignsAPI = {\n  getAll: (limit = 50, offset = 0, status = null) =>\n    api.get('/campaigns', { params: { limit, offset, status } }),\n  getById: (id) => api.get(`/campaigns/${id}`),\n  create: (data) => api.post('/campaigns', data),\n  update: (id, data) => api.put(`/campaigns/${id}`, data),\n  send: (id) => api.post(`/campaigns/${id}/send`),\n  delete: (id) => api.delete(`/campaigns/${id}`),\n};\n\n// Notifications API\nexport const notificationsAPI = {\n  getAll: (limit = 50, offset = 0, status = null) =>\n    api.get('/notifications', { params: { limit, offset, status } }),\n  getById: (id) => api.get(`/notifications/${id}`),\n  getByCustomer: (customerId) =>\n    api.get(`/notifications/customer/${customerId}`),\n  getByCampaign: (campaignId) =>\n    api.get(`/notifications/campaign/${campaignId}`),\n  updateStatus: (id, status) =>\n    api.put(`/notifications/${id}/status`, { status }),\n};\n\n// Analytics API\nexport const analyticsAPI = {\n  getCampaignMetrics: (campaignId) =>\n    api.get(`/analytics/campaigns/${campaignId}`),\n  getEngagementOverview: () =>\n    api.get('/analytics/engagement/overview'),\n  getSegmentEngagement: () =>\n    api.get('/analytics/segments/engagement'),\n};\n\nexport default api;\n